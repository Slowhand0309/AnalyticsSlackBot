{
  "name": "analytics-slack-bot",
  "version": "1.0.0",
  "scripts": {
    "build": "rm -rf dist && webpack && cp src/appsscript.json dist",
    "deploy": "yarn build && clasp push"
  },
  "dependencies": {
    "@types/google-apps-script": "^0.0.29",
    "typescript": "^3.1.3"
  },
  "devDependencies": {
    "@types/node": "^10.11.7",
    "gas-webpack-plugin": "^0.3.0",
    "ts-loader": "^5.2.1",
    "ts-node": "^7.0.1",
    "tslint": "^5.11.0",
    "webpack": "^4.20.2",
    "webpack-cli": "^3.1.2"
  }
}
